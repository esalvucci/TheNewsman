<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="drawerArrowStyle">@style/DrawerArrowStyle</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="DrawerArrowStyle" parent="Widget.AppCompat.DrawerArrowToggle">
        <item name="spinBars">true</item>
        <item name="color">@color/colorPrimaryDark</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat">
        <item name="color">@color/colorPrimaryDark</item>
    </style>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="ButtonStyle" parent="Base.Widget.AppCompat.Button.Colored">
        <item name="colorButtonNormal">@color/colorPrimaryDark</item>
    </style>

    <declare-styleable name="ChristmasTree">
        <attr name="light" format="color" />
        <attr name="medium" format="color" />
        <attr name="dark" format="color" />
    </declare-styleable>

    <style name="green_level" parent="DefaultLevel">
        <item name="light" format="color">#66ff00</item>
        <item name="medium" format="color">#44aa00</item>
        <item name="dark" format="color">#225500</item>
    </style>

    <style name="DefaultLevel">
        <item name="light" format="color">#cde2f6</item>
        <item name="medium" format="color">@color/colorPrimary</item>
        <item name="dark" format="color">@color/colorPrimaryDark</item>
    </style>

    <style name="MyButtonStyle" parent="Widget.AppCompat.Button.Colored">
        <item name="android:colorButtonNormal">@color/colorPrimary</item>
        <item name="android:textColor">@color/colorAccent</item>
    </style>
</resources>